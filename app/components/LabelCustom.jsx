import React from "react";
import PropTypes from "prop-types";

function LabelCustom({ children }) {
  return <span className="label">{children}</span>;
}

LabelCustom.propTypes = {};

export default LabelCustom;
